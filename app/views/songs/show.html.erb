<h1><%= @song.title %></h1>

<p><%=  @song.duration %></p>

<%= link_to "[Edit]", edit_artist_song_path %>

<%= link_to "[Delete]", artist_song_path(@song.id), method: :delete, data: { confirm: "Are you sure?" }%>

<%= link_to "[Back]", artist_path(@song.artist.id) %>
