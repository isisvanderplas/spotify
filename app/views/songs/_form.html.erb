
<%= form_for @song do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :duration %>
  <%= f.number_field :duration %>

  <%= f.label :artist %>
  <%= f.text_field :artist %>

  <%= f.submit (@song.new_record? ? "Create" : "Update") %>

<% end %>
