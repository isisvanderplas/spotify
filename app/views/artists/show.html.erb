<h1><%= @artist.name %></h1>

<h4>List of songs:</h4>
<% @artist.songs.each do |song| %>
<p><%= song.title %></p>
<% end %>

<%= link_to "[Edit]", edit_artist_path %>

<%= link_to "[Back]", artists_path %>
